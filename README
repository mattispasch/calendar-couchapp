This CouchApp contains views for working with calendars.

Kanso:

sudo npm install -g kanso
kanso install
kanso push http://localhost:5984/{db_name}


Requirements:

Linux Mint/Ubuntu:

apt-get install npm couchdb
(TODO) ln -s /usr/bin/nodejs /usr/bin/node